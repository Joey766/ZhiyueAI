// These links are populated with the verified repository and Pages URLs during release.
const GITHUB_URL = "#";
document.querySelectorAll("#github-link, #github-hero-link").forEach((link) => { link.href = GITHUB_URL; });
